<h1>Restaurants</h1>

<p>
  <%= link_to "Add new restaurant", new_restaurant_path, class: "btn btn-secondary" %>
</p>

<% @restaurants.each do |restaurant| %>
  <%= render "restaurant", restaurant: restaurant %>
  <%= link_to "See details", restaurant_path(restaurant) %>
<% end %>
